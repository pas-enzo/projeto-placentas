# --- Python ---
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
env/

# --- Jupyter ---
.ipynb_checkpoints/

# --- OS ---
.DS_Store
Thumbs.db

# --- Large ML / training artifacts ---
# Lightning checkpoints (RF-DETR training); often 500MB+ each
*.ckpt

# RF-DETR: Roboflow-downloaded base weights + anything under training runs
v2_rf_detr/rf-detr*.pt
v2_rf_detr/runs/*
!v2_rf_detr/runs/.gitkeep

# RF-DETR optimization workspace artifacts (keep notebook/code, skip heavy outputs)
v2_rfdetr_opt_v1/artifacts/runs/*
v2_rfdetr_opt_v1/notebooks/rf-detr-seg-small.pt

# RF-DETR Seg Large optimization (keep notebook/viz/reports/benchmarks; move .pth via scp)
v2_rfdetr_seg_large_opt_v1/artifacts/runs/**/*.pth
yolo11n-seg.pt

# Generic RF-DETR-style checkpoints (YOLO weights use .pt and are handled separately)
*.pth
