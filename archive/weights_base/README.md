# Base weights (local only)

Download / copy `.pt` files here. This directory is gitignored (`*.pt` + `archive/weights_base/`).

Expected names used by notebooks:

- `yolo11s-seg.pt` — Ultralytics YOLO11s-seg (capilar v4 / macenko)
- `yolo26s-seg.pt` — YOLO26s-seg (ablation notebook 04)
- `rf-detr-seg-large.pt` — RF-DETR Seg Large base (optional local cache)
- `yolov8*.pt` — early experiments only
